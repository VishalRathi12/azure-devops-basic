trigger:
- main

stages:
- stage: BasicStage
  jobs:
  - job: ReadFileJob
    pool:
      name: Default   # 👈 THIS IS THE KEY FIX
    steps:
    - script: |
        echo "Pipeline started"
        cat note.txt
        echo "Pipeline finished"
      displayName: 'Read text file'

